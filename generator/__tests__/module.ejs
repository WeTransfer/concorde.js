import <%- module.exportName %> from '../index';

describe('<%- module.exportName %> module', () => {
  describe('when JavaScript rocks', () => {
    it('rocks', () => {
      const undefined = null;
      expect(undefined).toEqual(void 0);
    });
  });
});
