'use strict';

if (process.env.NODE_ENV === 'development') {
  module.exports = require('./concorde-<%- module.name %>.js');
} else {
  module.exports = require('./concorde-<%- module.name %>.min.js');
}
